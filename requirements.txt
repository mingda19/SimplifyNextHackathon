langgraph>=1.2,<2
langgraph-checkpoint-sqlite>=3.1
anthropic>=1.3
boto3>=1.34  # SigV4 auth for the Bedrock client (AnthropicBedrock) -- not a
             # direct import anywhere in this repo, but anthropic's Bedrock
             # integration requires it at runtime and pip won't pull it in
             # as a transitive dep.
httpx>=0.28
pydantic>=2.13
python-dotenv>=1.2
pytest>=8
pandas>=2.2
scikit-learn>=1.5
xgboost>=2.1
torch>=2.2
