langgraph>=1.2,<2
langgraph-checkpoint-sqlite>=3.1
# Direct now, not transitive-only -- see services/orchestrator/requirements.txt
# for why (ToolNode/tools_condition/InjectedState, AIMessage/ToolMessage/
# MessagesState/add_messages, the @tool decorator are all imported directly).
langgraph-prebuilt>=1.1,<2.0
langchain-core>=1.6,<2.0
anthropic[bedrock]>=1.3
-e .
httpx>=0.28
pydantic>=2.13
python-dotenv>=1.2
pytest>=8
pandas>=2.2
scikit-learn>=1.5
xgboost>=2.1
torch>=2.2
