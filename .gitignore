data/*.csv
.env

# AWS — credentials NEVER get committed. aws/config has no secrets and is
# tracked so the whole team resolves the same SSO profile.
aws/credentials
aws/sso/
aws/cli/
!aws/config

# runtime artefacts — never commit
*.db
spend.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Build and tooling output
build/
dist/
*.egg-info/
.mypy_cache/
.ruff_cache/

