fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.2,<4.0
alembic>=1.14,<2.0
pydantic-settings>=2.7,<3.0

# Kept with the service dependencies so the container can run its own smoke
# and integration tests during the hackathon.
httpx>=0.27,<1.0
pytest>=8.3,<9.0
