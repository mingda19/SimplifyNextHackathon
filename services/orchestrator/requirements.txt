fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
psycopg2-binary>=2.9,<3.0
langgraph>=1.2,<2.0
langgraph-checkpoint-sqlite>=3.1,<4.0
# Previously undeclared transitive deps of langgraph itself -- now imported
# directly (ToolNode/tools_condition/InjectedState, AIMessage/ToolMessage/
# MessagesState/add_messages, the @tool decorator), so they must be direct,
# pinned lines here. Relying on a transitive pull is a landmine: a future
# bump elsewhere in the tree that happens to drop it would break the
# orchestrator with an ImportError nobody sees coming from a requirements
# diff.
langgraph-prebuilt>=1.1,<2.0
langchain-core>=1.6,<2.0
anthropic[bedrock]>=1.3
httpx>=0.28,<1.0
python-dotenv>=1.2,<2.0
pytest>=8.3,<9.0
